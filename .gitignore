# Environment variables & secrets — NEVER commit API keys
.env
.env.*
!.env.example

# Python caches
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Virtual environment
venv/
.venv/
env/

# NOTE: models/*.pkl are intentionally NOT ignored.
# They are regenerated by train_model.py during Render build.
# Committing them avoids re-training on every cold-start if needed.
